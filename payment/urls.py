# payment/urls.py

from django.urls import path
from . import views  # Import the views module if needed

urlpatterns = [
    #path('payment/', views.payment_view, name='payment'),
    # Add more paths as needed for your views
]
